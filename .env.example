# Server Port (Render will automatically override this)
PORT=5000

# MongoDB Database Connection String
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/cyberguardians?retryWrites=true&w=majority

# JWT Secret Key for token validation
JWT_SECRET=your_super_secure_jwt_secret_key_here
